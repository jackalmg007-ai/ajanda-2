{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Ana pencere için varsayılan izinler",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
